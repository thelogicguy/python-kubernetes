apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-app-project.fullname" . }}
  labels:
    {{- include "python-app-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-app-project.selectorLabels" . | nindent 4 }}
